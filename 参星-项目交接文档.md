# 参星（Trinary）— 项目交接文档

> 本文档供其他 AI 模型阅读后快速理解参星项目全貌，并投入后续开发工作。
> 理论模型的完整细节请参阅 `参星-理论模型-终稿.md`。

---

## 一、项目一句话

参星是一个**基于天文隐喻的项目知识图谱系统**——用星云、恒星、星团、旋臂、宇宙网这些真实天文结构来组织知识，让知识的组织方式本身成为知识的一部分。

---

## 二、你需要知道的核心概念

### 知识图谱的五层结构

```
星云（知识原料）→ 恒星系统（知识单元）→ 星团（知识聚类）→ 星系（知识全景）→ 宇宙网（跨域关联）
```

### 知识单元的两个独立维度

- **生命周期**（成熟度）：原恒星 → 主序星 → 红巨星 → 白矮星 / 超新星
- **知识层次**（内容性质）：事实 / 决策 / 洞察

两者正交，自由组合。

### 知识循环

```
星云 → 原恒星 → 主序星 → 红巨星 → 超新星 → 回归星云
```

超新星散落的洞察滋养下一代恒星。知识在循环中不断提炼。

### 三个关键设计决策

1. **星座概念已废弃**：用"星团"替代，因为星座是人为投影，星团是物理真实的聚合
2. **旋臂是密度波**：不是预设路径，而是由知识间的关联密度自动涌现
3. **AI 是可选的**：参星不依赖 AI，纯手动模式完全可用

---

## 三、技术栈选型（已确定）

| 层 | 选型 | 说明 |
|---|---|---|
| **意图初筛** | MiMo-V2-Flash | $0.10/M token，146 tok/s，快且便宜 |
| **深度分析** | MiMo-V2.5-Pro | Agent 能力最强，Token 效率最优 |
| **延迟修正** | DeepSeek-V3.2 | 通识推理强，补 MiMo 短板 |
| **本地备用** | Qwen3-32B | 离线可用，中文强 |
| **对话前端** | Dify / 自建 | 可替换 |
| **星图前端** | Canvas / WebGL | 独立可视化 |
| **事件总线** | Webhook + Redis Streams | 三层过滤 |
| **数据层** | PostgreSQL | 图谱数据存储 |

模型通过 OpenAI 兼容 API 网关接入，可热切换。

---

## 四、三层过滤机制

不是每条对话都处理，而是层层过滤：

```
Layer 1：规则预过滤（零成本）
    过滤短消息（≤3字）、应答词（嗯/好/继续）、表情
    通过率 ~50%

Layer 2：意图初筛（MiMo-V2-Flash，低成本）
    分类：推进/理解/发散/应答
    应答类丢弃
    通过率 ~25%

Layer 3：深度分析（MiMo-V2.5-Pro，按需调用）
    知识提取 + 生命周期判定 + 系统归属 + 关系建立
```

日成本降至全量处理方案的 ~4%。

---

## 五、当前进度

### 已完成

- [x] 理论模型设计（v6 终稿，含全部迭代）
- [x] 天文隐喻体系确定（去掉星座，用星团+旋臂+宇宙网）
- [x] 知识图谱数据结构设计（五层结构 + 两个正交维度 + 知识循环）
- [x] 模型选型确定（MiMo + DeepSeek + Qwen）
- [x] 三层过滤机制设计
- [x] 技术架构设计（双前端分离 + 事件驱动）

### 待推进

以下任务按优先级排列：

#### Phase 1：数据层

- [ ] PostgreSQL 数据库 schema 设计
  - 星云表、恒星系统表、行星表、卫星表
  - 星团表、旋臂表、宇宙网纤维表
  - 关系表（恒星间、行星间、系统间）
  - 历史记录表、编辑日志表
  - 用户/贡献者表、权限表
- [ ] 数据迁移方案（从对话记录到图谱的导入工具）

#### Phase 2：星系引擎

- [ ] Layer 1 规则过滤器实现
- [ ] Layer 2 意图初筛模型接入（MiMo-V2-Flash API）
- [ ] Layer 3 深度分析模型接入（MiMo-V2.5-Pro API）
- [ ] 知识提取逻辑（从对话中提取知识片段）
- [ ] 生命周期判定逻辑
- [ ] 知识层次判定逻辑
- [ ] 恒星系统创建/更新逻辑
- [ ] 星团聚类算法
- [ ] 旋臂密度波计算
- [ ] 延迟修正引擎（异步回溯）
- [ ] 隐性关联发现（语义相似度计算）

#### Phase 3：事件总线

- [ ] Webhook 接收端
- [ ] 三层过滤 pipeline
- [ ] 消息队列（Redis Streams）
- [ ] 与对话前端的对接（Dify 插件 / 自建对接）

#### Phase 4：对话前端

- [ ] Dify 集成 / 自建对话 UI
- [ ] 对话消息实时推送到事件总线
- [ ] 用户手动创建节点的交互
- [ ] 纠偏交互（拖拽、类型切换、关系编辑）

#### Phase 5：星图前端

- [ ] Canvas/WebGL 渲染引擎
- [ ] 全景视图（星系轮廓、星团位置、旋臂）
- [ ] 星团视图（星团内恒星系统、关系连线）
- [ ] 恒星系统视图（恒星+行星+卫星、行星间关联）
- [ ] 恒星表面视图（知识内容全文、历史记录）
- [ ] 多尺度视图切换（平滑过渡）
- [ ] 视觉映射实现
  - 生命周期 → 亮度/颜色/稳定性
  - 知识层次 → 形状/图标
  - 星团类型 → 形状/颜色（疏散=不规则蓝、球状=球形红）
  - 旋臂 → 渐变亮度曲线
  - 宇宙网纤维 → 实线/虚线、粗细
- [ ] 搜索/筛选 UI
- [ ] 拖拽交互
- [ ] 时间线回放

#### Phase 6：知识库能力

- [ ] 全文搜索引擎
- [ ] 知识复用（导入、引用、模板提取）
- [ ] 导出功能（Markdown、JSON、图片、知识卡片）
- [ ] 图谱快照（任意时间点状态查看）

#### Phase 7：协作能力

- [ ] 多用户支持
- [ ] 编辑历史与版本控制
- [ ] 星团权限管理（公共/私有/共享）
- [ ] AI 操作的审计日志

---

## 六、开发建议

### 从哪里开始

**建议从 Phase 1（数据层）开始**。数据结构是一切的基础，schema 确定后其他层才能开发。

### 核心数据结构参考

```sql
-- 恒星系统（核心表）
CREATE TABLE stellar_systems (
  id UUID PRIMARY KEY,
  name VARCHAR(255),
  star_title VARCHAR(500),
  star_summary TEXT,
  star_content TEXT,
  lifecycle VARCHAR(20) CHECK (lifecycle IN ('protostar', 'main_sequence', 'red_giant', 'white_dwarf', 'supernova')),
  knowledge_layer VARCHAR(20) CHECK (knowledge_layer IN ('fact', 'decision', 'insight')),
  brightness FLOAT DEFAULT 0.5,
  mass FLOAT DEFAULT 0.5,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 星云
CREATE TABLE nebulae (
  id UUID PRIMARY KEY,
  name VARCHAR(255),
  content TEXT,
  source VARCHAR(50),
  density FLOAT DEFAULT 0.1,
  absorbed_by UUID REFERENCES stellar_systems(id),
  created_at TIMESTAMP DEFAULT NOW()
);

-- 星团
CREATE TABLE clusters (
  id UUID PRIMARY KEY,
  name VARCHAR(255),
  type VARCHAR(20) CHECK (type IN ('open', 'globular')),
  center_system_id UUID REFERENCES stellar_systems(id),
  density FLOAT,
  age VARCHAR(20),
  visibility VARCHAR(20) DEFAULT 'public',
  created_at TIMESTAMP DEFAULT NOW()
);

-- 恒星系统与星团的多对多关系
CREATE TABLE system_cluster_map (
  system_id UUID REFERENCES stellar_systems(id),
  cluster_id UUID REFERENCES clusters(id),
  PRIMARY KEY (system_id, cluster_id)
);

-- 行星（派生知识）
CREATE TABLE planets (
  id UUID PRIMARY KEY,
  system_id UUID REFERENCES stellar_systems(id),
  title VARCHAR(500),
  summary TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 卫星（细节知识）
CREATE TABLE satellites (
  id UUID PRIMARY KEY,
  planet_id UUID REFERENCES planets(id),
  content TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 行星间关联
CREATE TABLE planet_relations (
  from_planet UUID REFERENCES planets(id),
  to_planet UUID REFERENCES planets(id),
  relation_type VARCHAR(20),
  PRIMARY KEY (from_planet, to_planet)
);

-- 恒星系统间关系
CREATE TABLE system_relations (
  from_system UUID REFERENCES stellar_systems(id),
  to_system UUID REFERENCES stellar_systems(id),
  relation_type VARCHAR(20),
  strength FLOAT DEFAULT 0.5,
  direction VARCHAR(10) DEFAULT 'bidirectional',
  PRIMARY KEY (from_system, to_system)
);

-- 宇宙网纤维（跨域关联）
CREATE TABLE cosmic_web_fibers (
  id UUID PRIMARY KEY,
  from_system UUID REFERENCES stellar_systems(id),
  to_system UUID REFERENCES stellar_systems(id),
  fiber_type VARCHAR(20) CHECK (fiber_type IN ('explicit', 'implicit')),
  strength FLOAT,
  source VARCHAR(20),
  created_at TIMESTAMP DEFAULT NOW()
);

-- 历史记录
CREATE TABLE system_history (
  id UUID PRIMARY KEY,
  system_id UUID REFERENCES stellar_systems(id),
  event VARCHAR(100),
  detail TEXT,
  timestamp TIMESTAMP DEFAULT NOW()
);

-- 编辑日志
CREATE TABLE edit_log (
  id UUID PRIMARY KEY,
  entity_type VARCHAR(50),
  entity_id UUID,
  user_id VARCHAR(100),
  action VARCHAR(50),
  detail TEXT,
  source VARCHAR(10) DEFAULT 'user',  -- 'user' or 'ai'
  timestamp TIMESTAMP DEFAULT NOW()
);
```

### API 设计方向

建议采用 RESTful API + WebSocket（实时推送）：

```
GET    /api/nebulae                    # 列出星云
POST   /api/nebulae                    # 创建星云
POST   /api/nebulae/:id/condense       # 星云凝聚为恒星

GET    /api/systems                    # 列出恒星系统
POST   /api/systems                    # 创建恒星系统
GET    /api/systems/:id                # 获取恒星系统详情
PATCH  /api/systems/:id                # 更新恒星系统
GET    /api/systems/:id/history        # 获取历史记录
GET    /api/systems/:id/planets        # 获取行星列表

GET    /api/clusters                   # 列出星团
POST   /api/clusters                   # 创建星团
GET    /api/clusters/:id/systems       # 获取星团内系统

GET    /api/galaxy                     # 获取星系全景
GET    /api/galaxy/spiral-arms         # 获取旋臂
GET    /api/galaxy/cosmic-web          # 获取宇宙网

GET    /api/search?q=...               # 全文搜索
GET    /api/snapshot?timestamp=...     # 图谱快照

POST   /api/events/message             # 对话消息推送（触发三层过滤）
POST   /api/events/confirm             # 用户确认 AI 操作
POST   /api/events/reject              # 用户拒绝 AI 操作

WebSocket /ws/galaxy                   # 实时图谱更新推送
```

### 前端技术选型建议

| 组件 | 推荐 | 理由 |
|---|---|---|
| 渲染引擎 | PixiJS 或 Three.js | Canvas/WebGL，性能好 |
| 2D 图谱 | PixiJS（2D Canvas） | 简单直接，适合初期 |
| 3D 图谱（后期） | Three.js | 支持三维星图 |
| UI 框架 | React / Vue | 交互组件 |
| 布局算法 | d3-force / 自研 | 力导向布局，适合图谱 |

---

## 七、注意事项

### 设计红线

1. **AI 可选**：任何功能都必须在纯手动模式下可用，AI 是增强不是前提
2. **知识图谱是本体**：不要把参星做成"AI 的记忆库"，它是用户的知识图谱
3. **星座已废弃**：用"星团"替代，不要再引入星座概念
4. **旋臂是涌现的**：不是预设路径，由关联密度自动计算
5. **两个维度正交**：生命周期和知识层次是独立的，不要混为一谈
6. **知识是循环的**：超新星散落的洞察要回到星云层

### 常见误区

- ❌ 把参星做成"AI 对话管理系统" → 参星是知识图谱，AI 对话只是输入源之一
- ❌ 把星云当垃圾箱 → 星云是知识原料池，是恒星的诞生地
- ❌ 预设旋臂路径 → 旋臂应该由密度波自动涌现
- ❌ 每条对话都处理 → 三层过滤，只处理有价值的消息
- ❌ 节点只能属于一个聚类 → 一个恒星系统可以属于多个星团

---

## 八、相关文件

| 文件 | 说明 |
|------|------|
| `参星-理论模型-终稿.md` | 完整理论模型，包含所有设计细节 |
| `参星-项目交接文档.md` | 本文件，供其他 AI 模型快速上手 |

---

## 九、开始工作前的 Checklist

- [ ] 阅读 `参星-理论模型-终稿.md`，理解五层结构和两个正交维度
- [ ] 理解"星座已废弃，用星团替代"的原因
- [ ] 理解"旋臂是密度波，不是预设路径"的含义
- [ ] 理解"AI 可选"的设计原则
- [ ] 理解"知识循环"：星云→恒星→超新星→星云
- [ ] 确认技术栈选型（PostgreSQL + MiMo + Canvas/WebGL）
- [ ] 从 Phase 1（数据层）开始开发

---

*文档版本：v1.0*
*生成日期：2026-05-25*
